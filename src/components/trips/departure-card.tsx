import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function DepartureCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Departure</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-slate-600">Departure card placeholder for Phase 1.</p>
      </CardContent>
    </Card>
  );
}
