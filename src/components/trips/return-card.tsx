import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ReturnCard() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Return</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-sm text-slate-600">Return card placeholder for Phase 1.</p>
      </CardContent>
    </Card>
  );
}
